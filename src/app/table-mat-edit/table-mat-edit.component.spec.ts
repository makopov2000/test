import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TableMatEditComponent } from './table-mat-edit.component';

describe('TableMatEditComponent', () => {
  let component: TableMatEditComponent;
  let fixture: ComponentFixture<TableMatEditComponent>;

  //beforeEach(async(() => {
  //  TestBed.configureTestingModule({
  //    declarations: [ TableMatEditComponent ]
  //  })
  //  .compileComponents();
  //}));

  //beforeEach(() => {
  //  fixture = TestBed.createComponent(TableMatEditComponent);
  //  component = fixture.componentInstance;
  //  fixture.detectChanges();
  //});

  //it('should create', () => {
  //  expect(component).toBeTruthy();
  //});
});
